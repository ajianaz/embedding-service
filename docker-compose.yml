version: "3.8"

services:
  embedding_service:
    build: .
    container_name: embedding_service
    ports:
      - "5002:5001"
    volumes:
      - ./app:/app  # Sinkronisasi folder `app` dengan container
    restart: always